import React from "react";
import Logo from '../../../img/logo-login.png'

export const LogoLogin = () => (
    <div className="login">
        <form>
            <img src= {Logo} />
        </form>
    </div>
)